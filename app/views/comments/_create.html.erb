<%= form_for :comment do |f| %>
  <p>
    <%= f.label :text, "Напишите свой отзыв:" %><br />
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.label :name, "Представтесь:" %><br />
    <%= f.text_field :name %>
  </p>

  <%= f.submit "Отправить", :id => "submit" %>
<% end %>
